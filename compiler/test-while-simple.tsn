// Phase 23 Test: simpler while loop

function main(): i32 {
    let x: i32 = 0;
    let count: i32 = 5;
    
    while (count) {
        x = x + 1;
        count = count - 1;
    }
    
    return x;
}
