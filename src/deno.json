{
  "name": "tsn-compiler",
  "version": "0.11.0",
  "tasks": {
    "compile": "deno run --allow-read --allow-write --allow-run src/main.ts",
    "concat": "deno run --allow-read --allow-write src/concat-modules.ts"
  },
  "compilerOptions": {
    "strict": true,
    "lib": ["esnext","deno.ns", "dom"]
  }
}
