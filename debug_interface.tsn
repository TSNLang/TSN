interface ASTNode {
    kind: i32;
    value1: i32;
    value2: i32;
    value3: i32;
}

function test_interface(nodes: ptr<ASTNode>, idx: i32): void {
    let nameStart = nodes[idx].value1;
    let nameLen = nodes[idx].value2;
}