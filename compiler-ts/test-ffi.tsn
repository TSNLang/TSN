// Test FFI declarations
@ffi.lib("kernel32")
declare function GetStdHandle(nStdHandle: i32): ptr<void>;

@ffi.lib("kernel32")
declare function WriteConsoleA(
    hConsoleOutput: ptr<void>,
    lpBuffer: ptr<void>,
    nNumberOfCharsToWrite: u32,
    lpNumberOfCharsWritten: ptr<u32>,
    lpReserved: ptr<void>
): bool;

function print(msg: ptr<i8>): void {
    let stdHandle: i32 = 0;
    stdHandle = stdHandle - 11;
    let hConsole: ptr<void> = GetStdHandle(stdHandle);
    
    let len: u32 = 5;
    let written: u32;
    let writtenPtr: ptr<u32> = addressof(written);
    
    WriteConsoleA(hConsole, msg, len, writtenPtr, null);
}

function main(): i32 {
    return 0;
}
