import * as console from "std:console";

function main() {
    let arr: i32[10];
    arr[0] = 42;
    arr[1] = 100;
    
    let val = arr[0];
    
    if (val == 42) {
        console.log("Array test PASSED!");
    } else {
        console.log("Array test FAILED!");
    }
}
