import * as console from "std:console";

interface Token {
    kind: i32;
    pos: i32;
}

function main() {
    let tok: Token;
    tok.kind = 5;
    tok.pos = 10;
    
    let k = tok.kind;
    let p = tok.pos;
    
    console.log("Member access test");
}
