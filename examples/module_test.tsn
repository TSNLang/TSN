// Test importing from another module

import { add, multiply } from "./math_module.tsn";

function main(): i32 {
    let x = add(5, 3);
    let y = multiply(4, 7);
    let result = add(x, y);
    return result;
}
